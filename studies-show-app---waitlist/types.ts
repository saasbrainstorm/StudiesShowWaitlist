export interface ResearchCardData {
  id: string;
  category: string;
  headline: string;
  summary: string;
  rotation: number;
}

export interface Position {
  top?: string;
  left?: string;
  right?: string;
  bottom?: string;
}